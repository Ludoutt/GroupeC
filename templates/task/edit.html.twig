{% extends 'base.html.twig' %}

{% block title %}Edit Project{% endblock %}

{% block body %}
    <h1>Edit Task</h1>

    {{ include('task/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('backlog', {'project': project.id}) }}">back to list</a>

    {{ include('task/_delete_form.html.twig') }}
{% endblock %}
